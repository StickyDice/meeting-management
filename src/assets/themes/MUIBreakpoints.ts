import { createTheme } from "@mui/material";

export const MUIBreakpoints = createTheme({
   breakpoints: {
      values: {
         xs: 0,
         sm: 710,
         md: 900,
         lg: 1200,
         xl: 1536,
      },
   },
});
